{
  "name": "llm-vs-house",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "LLM vs House — a behavioral study of how LLMs take risk on negative-expectation casino games. Simulated points only, no real gambling.",
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "build:packages": "pnpm --filter @casino/engine build && pnpm --filter @casino/core build && pnpm --filter @casino/llm build",
    "build": "pnpm build:packages && pnpm --filter @casino/web build"
  },
  "devDependencies": {
    "playwright-core": "^1.61.1",
    "typescript": "^5.7.3"
  }
}
